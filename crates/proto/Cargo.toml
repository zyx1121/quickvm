[package]
name = "quickvm-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
postcard = { version = "1.1.3", features = ["use-std"] }
quickvm-event = { version = "0.1.0", path = "../event" }
serde = { version = "1.0.228", features = ["derive"] }
